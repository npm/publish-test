# TODO if template-oss config asks for it

runs-on: ubuntu-latest
defaults:
  run:
    shell: bash
permissions:
  deployments: write
steps:
  - name: See what's what
    run: |
      git status
      ls
      npm -v
      node -v
